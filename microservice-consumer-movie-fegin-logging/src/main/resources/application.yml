server:
  port: 8010

spring:
  application:
    name: microservice-consumer-movie-fegin

eureka:
  client:
    service-url:
      default-zone: http://peer1:8761/eureka/,http://peer2:8762/eureka/
  instance:
    prefer-ip-address: true
logging:
  level:
    pers.zy.springcloud.study.client.UserClient: debug # Fegin 的Logger.Level 只对DEBUG做出响应


